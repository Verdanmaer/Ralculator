const InputNumbers = ({ setValue }) => {
  return (
    <div>
      <button>+</button>
      <button>-</button>
      <button>÷</button>
      <button>x</button>
      <button>=</button>
    </div>
  );
};

export default InputNumbers;
